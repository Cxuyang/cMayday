<!--pages/classic/classic.wxml-->
<view class="container">
  <v-like islike="{{classic.like_status}}" count="{{classic.fav_nums}}"/>
  <v-movie img="{{classic.image}}" content="{{classic.content}}"/>
</view>
